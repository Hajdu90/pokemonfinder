gitbash telepítése a pc-re

Visual studio terminal
git config --global user.name "Hajdu90"
git config --global user.email "hajdu90shares@gmail.com"
git config --list

Github weblap, belépés, new reposit, megadjuk a nevét, létrehoz és onnan beillesztjük ezt a listát.
git init
git add .
git commit -m "upload a git.txt"
git branch -M main
git remote add origin https://github.com/Hajdu90/pokemonfinder.git
git push -u origin main

Új fájlok feltöltése
git add .
git commit -m "KOMMENT A FELTÖLTÉSHEZ"
git push

weblap
settings -> Pages -> 
Source: Deploy from a branch
Branch: main | /(root) | Save (nyomjuk meg)

(létrejött:
Your site is live at https://hajdu90.github.io/pokemonfinder/
Last deployed by @Hajdu90 Hajdu90 3 minutes ago)



Vesszük:

API-k, kiszolgálók, adatok lekérések,
api - végpont
json választ kapunk
promise
fetch() hálózati kérések
await - async


